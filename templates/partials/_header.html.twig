<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm border-bottom">
    <div class="container-fluid px-4 py-3">
        <!-- Logo à gauche -->
        <a class="navbar-brand fw-bold fs-4 text-dark" href="{{ path('app_login') }}">
            <i class="bi bi-mortarboard-fill me-2 text-primary"></i>AppTuteur
        </a>

        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMenu">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center gap-2">
                <li class="nav-item">
                    <a class="nav-link px-3 rounded text-dark {{ app.request.get('_route') == 'list_tuteurs_route' ? 'active bg-primary text-white' : '' }}" 
                       href="{{ path('list_tuteurs_route') }}">
                        <i class="bi bi-people me-1"></i>Tuteurs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 rounded text-dark" href="#">
                        <i class="bi bi-person-badge me-1"></i>Étudiants
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 rounded text-dark {{ app.request.get('_route') == 'app_sujets' ? 'active bg-primary text-white' : '' }}" 
                       href="{{ path('app_sujets') }}">
                        <i class="bi bi-book me-1"></i>Sujets
                    </a>
                </li>
                
                {% if app.session.get('tuteur_id') %}
                    <li class="nav-item">
                        <a class="nav-link px-3 rounded text-dark {{ app.request.get('_route') == 'app_dashboard' ? 'active bg-primary text-white' : '' }}" 
                           href="{{ path('app_dashboard') }}">
                            <i class="bi bi-speedometer2 me-1"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item ms-lg-2">
                        <a class="btn btn-outline-danger btn-sm" href="{{ path('app_logout') }}">
                            <i class="bi bi-box-arrow-right me-1"></i>Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item ms-lg-2">
                        <a class="btn btn-primary btn-sm px-4" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>